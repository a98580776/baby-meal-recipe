# PWA manifest + layout 연결 (TWA 준비)

- base commit: `8b8dd83`
- 아이콘 생성 병렬 세션과 격리 확인: `public/icons/` 미존재, 이번 작업에서 생성/수정 없음

## 1. 원격 DB/코드 실행 여부

없음. DB/migration/seed 변경 없음.

## 2. 생성/수정 파일 목록

작업 시작 시점에 이미 `public/manifest.json`(신규, untracked)과 `app/layout.tsx`(수정)가
워킹트리에 존재했음 — 이번 세션에서 내용을 새로 작성하지 않고 사양 대조만 수행, 그대로 유지.

`git diff --staged` (staged: `app/layout.tsx`, `public/manifest.json`):

```diff
diff --git a/app/layout.tsx b/app/layout.tsx
index 52dcb5f..0d0ac4f 100644
--- a/app/layout.tsx
+++ b/app/layout.tsx
@@ -1,4 +1,4 @@
-import type { Metadata } from "next";
+import type { Metadata, Viewport } from "next";
 import { Geist_Mono } from "next/font/google";
 import "@fontsource/pretendard/400.css";
 import "@fontsource/pretendard/500.css";
@@ -21,6 +21,13 @@ const geistMono = Geist_Mono({
 export const metadata: Metadata = {
   title: "Create Next App",
   description: "Generated by create next app",
+  manifest: "/manifest.json",
+};
+
+// metadata.themeColor is deprecated since Next.js 13.2 — theme-color now
+// lives on the separate `viewport` export (see generateViewport docs).
+export const viewport: Viewport = {
+  themeColor: "#5F6E2A",
 };
 
 export default function RootLayout({ children }: LayoutProps<"/">) {
diff --git a/public/manifest.json b/public/manifest.json
new file mode 100644
index 0000000..edd417b
--- /dev/null
+++ b/public/manifest.json
@@ -0,0 +1,48 @@
+{
+  "name": "이유식 레시피",
+  "short_name": "이유식 레시피",
+  "description": "재료와 아기 단계를 입력하면 손질부터 조리, 안전 주의사항까지 한 화면에서 확인하는 이유식 조리 도구",
+  "start_url": "/",
+  "display": "standalone",
+  "background_color": "#FBF7EE",
+  "theme_color": "#5F6E2A",
+  "lang": "ko",
+  "icons": [
+    {
+      "src": "/icons/icon-48.png",
+      "sizes": "48x48",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-72.png",
+      "sizes": "72x72",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-96.png",
+      "sizes": "96x96",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-144.png",
+      "sizes": "144x144",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-192.png",
+      "sizes": "192x192",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-512.png",
+      "sizes": "512x512",
+      "type": "image/png"
+    },
+    {
+      "src": "/icons/icon-512-maskable.png",
+      "sizes": "512x512",
+      "type": "image/png",
+      "purpose": "maskable"
+    }
+  ]
+}
```

`next.config.ts`: 변경 없음 (public/ 정적 서빙에 별도 설정 불필요 — 아래 4번 참조).

## 3. staged 파일 = 허용 3개 파일 대조

`git diff --staged --name-only`:

```
app/layout.tsx
public/manifest.json
```

`next.config.ts`는 diff 없음 → staged 대상 아님. `public/icons/` 관련 파일 staged 없음 (해당 디렉터리 자체가 미존재).

일치 여부: **일치** (허용 3개 중 실제 변경분 2개만 staged, 나머지 1개는 변경 불필요).

## 4. build / typecheck 결과

```
$ npm run typecheck
> tsc --noEmit
(exit 0, 출력 없음)
```

```
$ npm run build
▲ Next.js 16.3.1 (Turbopack)
✓ Compiled successfully in 10.9s
  Running TypeScript ...
  Finished TypeScript in 14.5s ...
✓ Generating static pages using 3 workers (13/13) in 1420ms

Route (app)
┌ ƒ /
├ ○ /_not-found
├ ƒ /api/v1/food-forms
├ ƒ /api/v1/ingredients
├ ƒ /api/v1/ingredients/[id]
├ ƒ /api/v1/recipes/generate
├ ƒ /api/v1/recipes/validate
├ ƒ /api/v1/stages
├ ○ /cooking
├ ƒ /plan
├ ○ /privacy
└ ○ /recipe
```

빌드 통과. 아이콘 PNG 파일 부재로 인한 런타임 404는 브라우저에서만 발생 (build 단계 실패 없음 — manifest.json 자체는 정적 파일이라 참조 유효성을 next build가 검사하지 않음).

정적 서빙 확인: `npm run start -- -p 3939` 로 프로덕션 서버 기동 후
`curl -s -o /dev/null -w "%{http_code}" http://localhost:3939/manifest.json` → `200`,
응답 본문이 `public/manifest.json` 내용과 일치 확인. 확인 후 서버 프로세스 종료.

## 5. commit 여부

**커밋하지 않음 — 승인 대기.** `app/layout.tsx`, `public/manifest.json` staged 상태로 유지.
이 보고서(`docs/claude-desktop-handoff/2026-09-10-pwa-manifest-twa.md`)만 워크플로우 규칙(§1)에 따라
승인 없이 commit + push함.

## 참고: 병렬 세션(아이콘) 격리

- `public/icons/` 디렉터리는 이번 세션에서 조회만 하고 생성/수정/삭제하지 않음 (미존재 상태 그대로).
- git add 시 `public/manifest.json app/layout.tsx next.config.ts` 3개만 명시적으로 add.
- `git status`의 다른 untracked 항목(`260824/broccoli/`, `scripts/__pycache__/`, `scripts/generate_tofu_doneness.py`)은 이번 작업과 무관 — 건드리지 않음.
